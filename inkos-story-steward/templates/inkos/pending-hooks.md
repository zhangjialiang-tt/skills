# InkOS 原生格式：pending_hooks.md

> 此模板用于编译包 `story-design/inkos/pending_hooks.md`。
> **必须使用 InkOS 表格格式**。InkOS 的 `parsePendingHooksMarkdown()` 首先尝试解析 Markdown 表格；
> 如果没有表格，会把每一个 `-` 开头的行当成独立伏笔并自动编号，导致结构破坏。
> 人类可读的完整谜题设计保留在 `07-foreshadowing-and-mystery.md`，机器文件只保存 InkOS 支持的字段。

---

## 待回收伏笔

| hook_id | 起始章节 | 类型 | 状态 | 最近推进 | 预期回收 | 回收节奏 | 上游依赖 | 回收卷 | 核心 | 半衰期 | 升级 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| H001 | 3 | mystery | planned | 0 | 仓库损耗真相：有人系统性偷窃物资建立私人储备 | 第20-25章 | 无 | 第一卷 | 是 | 8 | 否 | 前台问题：3号仓库异常损耗原因；公平线索：第三章接线被动过、第四章物资清点差异 |
| H002 | 5 | foreshadow | planned | 0 | 何卫国的军方背景将在第二卷揭示 | 第45-50章 | H001 | 第二卷 | 是 | 12 | 否 | 前台问题：何卫国为何对组织秩序有执念；公平线索：第五章签字习惯、第二章指挥风格 |

---

## 字段说明

| 字段 | 含义 |
|------|------|
| `hook_id` | 伏笔编号（H001 起） |
| `起始章节` | 埋设章节号（建书前填预期值） |
| `类型` | `mystery`（谜题）/ `foreshadow`（伏笔）/ `suspense`（悬念） |
| `状态` | `planned`（未埋设）/ `planted`（已埋设）/ `advancing`（推进中）/ `resolved`（已回收） |
| `最近推进` | 最近一次推进的章节号（0 = 尚未推进） |
| `预期回收` | 一句话描述回收内容 |
| `回收节奏` | 预期回收的章节范围 |
| `上游依赖` | 需要先完成的其他 hook_id（无 = 无依赖） |
| `回收卷` | 预期在哪一卷回收 |
| `核心` | 是否为核心伏笔（是/否） |
| `半衰期` | 读者好奇心衰减半衰期（章数，越大越耐等） |
| `升级` | 是否有升级版本（是/否） |
| `备注` | 前台问题、后台真相、公平线索等补充信息 |

---

## 使用说明

- 建书前：此文件作为初始伏笔种子写入编译包
- 建书后无章节且无 hooks.json：可直接修正此文件
- 建书后有章节：不直接修改，通过 current_focus.md 推进 + InkOS 结算
- **必须保持表格格式**，不要改成标题+列表格式（会导致解析错误）
- 编号递增，不跳号
- 每个伏笔的完整设计（误导、知识不对称、回收条件）保留在 `07-foreshadowing-and-mystery.md`
