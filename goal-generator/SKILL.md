---
name: goal-generator
version: "2.0.0"
description: >
  把用户提供的原始任务、需求或现象描述转换成高质量的 `/goal` 完成契约。
  支持两种 Profile：Standard（产品/功能/文档）和 Diagnostic（Bug/性能/硬件/根因调查）。
  当用户说"帮我生成 Goal"/"写一个 goal"/"构造 goal"/"把下面任务变成 goal"/
  "Goal 化"/"设计目标契约"/"帮我定义完成条件" 时，必须使用本 skill。
  即使任务看起来简单，只要用户想要一个可验证的完成条件而非即时执行，就应触发。
  不适合：纯一次性命令、未确定方向的探索、可直接用 Plan Mode 的明确任务、
         简单翻译、一行 shell 输出、纯头脑风暴。
triggers:
  include:
    - "帮我生成 Goal"
    - "写一个 goal"
    - "构造 goal"
    - "把下面任务变成 goal"
    - "Goal 化"
    - "设计目标契约"
    - "帮我定义完成条件"
    - "定义完成条件"
    - "需要 /goal"
  exclude:
    - "纯一次性命令"
    - "简单翻译"
    - "一行 shell"
    - "尚未确定方向的探索"
    - "已用 Plan Mode 的明确任务"
output_contract:
  artifacts:
    - "/goal 契约文档（内联输出）"
    - "可选：质量评分卡"
  format: "markdown"
  constraints:
    - "不执行 Goal 本身"
    - "不修改用户文件"
    - "不使用无依据的数字编造验收阈值"
---

# Goal Generator

把原始任务转换为适合 Coding Agent 持续执行的高质量 `/goal` 完成契约。

## Profile 自动选择

本 Skill 支持两种 Profile，根据任务特征自动选择，无需用户显式指定：

| Profile | 适用场景 | 逻辑字段 |
|---------|----------|----------|
| **Standard Goal** | 产品功能、文档交付、UI 修改、常规重构、小型工程、明确的新功能开发 | Outcome / Verification / Constraints / Boundaries / Iteration / Stop & Pause |
| **Diagnostic Goal** | Bug 定位、根因调查、性能优化、FPGA/RTL/嵌入式、可靠性问题、数据一致性、已有现象但根因未知 | Outcome / Current Facts / Hypotheses / Verification Evidence / Invariants & Anti-gaming / Work Boundaries / Experiment Strategy / Stop / Blocked Report |

### 判断规则

满足以下任一条件时，选择 **Diagnostic Goal**：

- 用户明确描述了一个失败现象（"不工作"、"报错"、"太慢"、"挂死"）
- 用户提出了一个未验证的根因猜测（"可能是 X 导致"、"我觉得是 Y"）
- 任务涉及硬件、板级、仿真 vs 上板一致性、时序、CDC
- 任务明确要求"找到根因"、"调查"、"复现"、"调试"
- 用户提供了可量化的失败指标（"每周 3-5 例"、"P95 太高"、"8 fps"）

以下情况选择 **Standard Goal**：

- 任务目标是创建新东西（功能、页面、文档、项目）
- 实现路径明确，验收条件可直接定义
- 没有失败现象或未验证猜测
- 任务本质是一次性修改或常规重构

如果信息不足以判断，先问自己：用户更关心"做出什么"还是"修好什么"？前者倾向 Standard，后者倾向 Diagnostic。仍然不确定时，默认 Standard 并显式声明假设。

---

## Standard Goal 契约

### 字段定义

| # | 字段 | 说明 |
|---|------|------|
| 1 | **Outcome** | 完成后系统是什么样（可观察状态，不是动作） |
| 2 | **Verification** | 每项 = 具体检查动作 + 通过条件（命令、测试、截图、日志、产物） |
| 3 | **Constraints** | 不能破坏什么（接口、行为、数据、风格、分支规则） |
| 4 | **Boundaries** | 允许修改 / 只读参考 / 禁止修改 |
| 5 | **Iteration Policy** | 一次一个聚焦改动，重跑检查，基于证据调整 |
| 6 | **Stop / Pause** | 完成条件 + 必须暂停的情形 |

### 质量门禁

| # | 检查项 | 失败时修正 |
|---|--------|-----------|
| 1 | Outcome 描述可观察状态（而非纯动作） | 改写为"完成后系统是什么样" |
| 2 | Verification 包含具体检查动作（命令/条件/截图/日志） | 补检查方式 |
| 3 | 用户提供了数字/指标时，Verification 有明确目标阈值 | 基于用户数字推导（如"每周 3-5 例"→"<1 例/周"） |
| 4 | Constraints 包含至少一项反投机约束 | 添加领域相关的禁止行为 |
| 5 | Boundaries 明确允许/禁止范围 | 补边界 |
| 6 | Stop 定义完成证据；Pause 列出高风险/需授权的情形 | 补停止或暂停条件 |

### 输出模板

```text
/goal [Outcome]。
Verification（验证）：[具体检查命令/条件 + 通过阈值或可观察条件]。
Constraints（约束）：[不能破坏什么]。
Boundaries（边界）：允许修改 [范围]；禁止修改 [范围]。
Iteration policy（迭代策略）：一次一个聚焦改动，重跑检查，基于证据调整。
Stop when（完成条件）：[哪些证据证明完成]。
Pause if（暂停条件）：[需要人工决定、凭证、付费、破坏性操作、生产数据、版权素材等]。
```

---

## Diagnostic Goal 契约

### 字段定义

| # | 字段 | 说明 |
|---|------|------|
| 1 | **Outcome** | 完成后系统是什么样（可观察状态） |
| 2 | **Current Facts** | 已确认的现象、测试结果、日志证据 |
| 3 | **Hypotheses** | 线索/猜测，但不得作为既定根因 |
| 4 | **Verification Evidence** | 每项 = 具体检查命令/条件 + 通过阈值 |
| 5 | **Invariants / Anti-gaming** | 不变量 + 领域相关反投机约束 |
| 6 | **Work Boundaries** | 允许修改 / 只读参考 / 禁止修改 |
| 7 | **Experiment Strategy** | 先复现 → 最小实验 → 单变量 → 局部验证 → 完整回归 |
| 8 | **Stop** | 全部验收证据满足时判定完成 |
| 9 | **Blocked Report** | 阻塞时必须报告的内容 |

### 质量门禁

| # | 检查项 | 失败时修正 |
|---|--------|-----------|
| 1 | 事实与假设严格分离 | 将假设移至 Hypotheses |
| 2 | Outcome 描述可观察状态 | 改写 |
| 3 | 每项 Verification Evidence 包含具体命令/条件 + 通过阈值 | 补命令和阈值 |
| 4 | 用户提供了指标时，Verification Evidence 有目标阈值 | 基于用户数字推导 |
| 5 | Invariants 包含至少一项领域相关反投机约束 | 添加 |
| 6 | Work Boundaries 明确允许/禁止范围 | 补边界 |
| 7 | Experiment Strategy 以可执行的复现为第一步 | 补复现步骤 |
| 8 | Stop 定义完成证据 | 补完成条件 |
| 9 | Blocked Report 列出至少 3 种阻塞情形及报告内容 | 补阻塞报告 |

### 输出模板

```text
/goal

【期望结果】
<完成后系统是什么样。不含"分析/研究/尝试"等动作词。>

【当前事实】
- <已经确认的现象/测试/日志>
- <当前失败现象>
- <可复现条件>

【待验证假设】
- <线索或猜测，但不得作为既定根因>

【验收证据】
1. <检查命令/脚本/测试>，通过阈值：<数值或可观察条件>
2. <检查命令/脚本/测试>，通过阈值：<数值或可观察条件>
3. <回归验证命令>，通过阈值：<原有测试全部 PASS>

【必须保持】
- <接口、行为、性能或兼容性不变量>
- <反投机约束：不通过删除测试/硬编码/屏蔽错误来虚假达成>

【工作边界】
允许修改：
- <路径或模块>

只读参考：
- <冻结基线或参考实现>

禁止修改：
- <明确的禁区>

【迭代策略】
- 先复现并建立证据基线；
- 优先执行能区分候选根因的最小实验；
- 一次只引入一个可归因变量；
- 修改后先运行最小相关测试，再运行完整回归；
- 根据证据更新假设，不重复无新证据支持的失败路径。

【阻塞与停止】
仅在所有验收证据满足时判定完成。
若环境不可用、必要信息缺失、约束冲突或无合理路径可继续，
停止修改并报告：
- 已尝试路径；
- 获得的证据；
- 当前判断及置信度；
- 未解决风险；
- 解除阻塞所需的最小输入。
```

---

## 关键规则

### 规则 1：事实 vs 假设分离（Diagnostic Goal 核心）

| 用户说的 | 属于 | 写入位置 |
|---------|------|---------|
| "仿真回归能通过" | 已确认事实 | 【当前事实】 |
| "data FIFO 出现 full" | 已确认事实 | 【当前事实】 |
| "可能是 status FIFO 导致" | 暂定假设 | 【待验证假设】 |
| "我觉得应该加个幂等锁" | 用户建议 | 【待验证假设】 |
| "因为网络超时导致重试" | 归因假设 | 【待验证假设】 |

**必须避免：**
- ❌ 把"可能是 status FIFO 导致"写成"根因是 status FIFO"
- ❌ 把"需要提高性能"写成具体的优化手段（如"增加三级 FIFO"）
- ❌ 把"不能破坏接口"写成模糊的"保持接口优雅"

### 规则 2：验收证据必须可执行

每项验收证据必须包含两个要素：
1. **检查动作**：具体的命令、脚本名、测试路径
2. **通过阈值**：明确的数值或可观察条件

```
❌ 模糊："测试通过"、"功能正确"、"代码质量良好"
✅ 可执行："pytest tests/test_payment.py -v 全部 PASS"
✅ 可执行："benchmarks/payment_bench.py P99 < 10ms"
✅ 可执行："重复扣款发生率 < 1 例/周（当前 3-5 例/周）"
```

### 规则 3：用户指标必须转化为验收阈值

**如果用户提到了具体数字（如"每周 3-5 例"、"延迟 X ms"、"N GB"、"8 fps"），验收证据中必须包含该指标的目标阈值，且必须基于用户数字推导。**

```
❌ 错误：用户说"每周 3-5 例" → 验收写"零重复扣款"（绝对化，无法验证渐进改善）
✅ 正确：用户说"每周 3-5 例" → 验收写"重复扣款 < 1 例/周（较当前降低 80%）"

❌ 错误：用户说"好几秒" → 验收写"搜索变快了"
✅ 正确：用户说"好几秒" → 验收写"P95 < 500ms（当前约 2-3s）"
```

### 规则 4：反投机约束必须领域具体

不能只写"不通过删除测试来虚假达标"这种泛泛之谈。必须根据任务领域写出具体的投机手段。

| 领域 | 具体反投机约束 |
|------|---------------|
| 支付/金融 | 不通过拒绝所有重复请求来达成"零重复扣款"；不通过合并多笔交易规避逐笔对账 |
| 性能优化 | 不通过减少返回结果数量、降低采样率、关闭核心功能来虚标性能提升 |
| 硬件/嵌入式 | 不通过降频、关闭校验模块、缩短测试时长来通过可靠性测试 |
| 搜索/推荐 | 不通过只返回热门结果、降低结果多样性来优化延迟或相关性指标 |
| 测试覆盖率 | 不通过删除边界条件测试、mock 外部依赖、写无意义断言来提高覆盖率 |

### 规则 5：迭代策略必须以可执行的复现为第一步（Diagnostic）

**"先复现"不是"先分析"。第一步必须包含可执行的操作（命令、脚本、场景），让人能稳定触发问题。**

```
❌ 错误："1. 分析现有日志，确认重复扣款的具体模式和根因"
✅ 正确："1. 在测试环境模拟网络超时（tc qdisc 注入延迟），触发客户端自动重试，确认至少复现 1 次重复扣款场景"

❌ 错误："1. 使用 ILA 抓取波形"
✅ 正确："1. 在 tb/read_path/tb_read_path_fifo_run.sv 中启动回归，连续运行 10 帧，确认 status FIFO empty 信号在 accepted < cmd_len 时被观察到"
```

---

## Default-First 策略

### 核心原则

低风险、低成本、可逆的不确定性，由 Skill 选择保守默认值，不拦用户填表。

只有当决策会实质改变以下内容时才追问：
- 产品方向
- 完成范围
- 成本
- 风险
- 所有权
- 数据安全
- 外部权限
- 不可逆操作
- 验收标准

### 默认策略必须根据任务类型判断

| 任务类型 | 默认策略 |
|---------|---------|
| 新产品原型 | 本地最小闭环，先做核心流程 |
| 存量工程 | 先读取已有文档、脚本、测试和约束 |
| Bug 调试 | 先复现和建立证据基线 |
| 陌生专业领域 | discovery-first（见下） |
| 发布、生产数据、凭证、付款、破坏性操作 | 不得默认执行，必须暂停 |

### 默认选择必须简短说明原因

```
默认选择理由：先做原创俯视 2D MVP，因为它能最快验证"城市移动、驾驶、任务"核心乐趣，同时避开版权和 3D 开放世界的高成本。
```

### 编号选择题（信息不足时使用）

```
可选调整
1. 项目形态：A 新建本地 MVP（默认） / B 改现有项目 / C 先做可交互原型
2. 范围：A 核心流程（默认） / B 加常见增强 / C 做完整产品
3. 验证：A 本地运行检查（默认） / B 真机或线上检查 / C 发布前检查

你可以直接回复：按默认，或回复类似 1B 2A 3C。
```

---

## Discovery-First 策略

对于陌生、专业或高风险领域：

- 不得编造领域规则
- Goal 应要求 Agent 先读取工作区中的权威信息
- 包括项目文档、规范、样例数据、现有实现、测试、运行脚本、冻结基线或用户提供材料
- 必要时列出工作假设
- 在证据不足时暂停，不得擅自固化领域结论

**Discovery-first 不能代替最终交付。** 除非任务本身只是调查，否则发现阶段之后仍应完成最小可验证目标。

```text
/goal 创建一个安全的医疗影像标注工具第一版，先读取工作区内的项目文档、样例数据说明和可用脚本，再实现最小可验证标注流程。
验证：识别并读取项目文档、样例数据说明、现有脚本或官方参考资料；运行最小相关检查；用样例数据完整走通一次导入、标注、保存、重新打开流程，并以日志、截图或导出文件作为证据。
约束：不编造医学结论、合规声明、数据语义或诊断用途；不处理生产患者数据；不改变未理解的数据格式。
边界：只修改第一版标注流程直接需要的界面、状态和样例数据处理文件；不触碰生产配置、凭证或无关模块。
迭代策略：先完成发现阶段并列出工作假设，再实现一个聚焦切片；每次失败后基于日志或文档调整，最多做 3 轮聚焦改进。
完成条件：样例数据下的最小标注流程有运行证据证明可用，检查通过或明确说明缺少配置，未解决的领域问题被列出。
暂停条件：需要医疗判断、合规审批、真实患者数据、外部付费服务、生产部署或破坏性数据迁移时暂停。
```

---

## 输出契约

### 信息充分时

优先输出：
1. 最终可复制 `/goal`
2. 简短质量摘要（仅在有重大风险或不确定时）

不要重复输出两个语义相同的长版本。

### 信息不充分但可以安全默认时

输出：
1. `推荐执行版（中文，可直接复制）`
2. `默认选择理由`
3. 最多 3～4 个真正影响方向的编号调整项
4. 用户可以回复"按默认"或对应选项

### 存在高风险或关键缺失时

输出：
1. 当前可生成的 discovery-first 或受限 Goal
2. 明确列出必须由用户决定的问题
3. 不得伪造最终契约

### 压缩版

只在以下情况生成：
- 用户明确要求
- 目标运行环境对长度有明显限制
- 完整版过长且压缩后不会丢失边界、验收和停止条件

压缩版不得删除核心安全约束。

### 质量评分

正式定义评分维度：
- Outcome 可观察性
- 验收可执行性
- 边界清晰度
- 事实与假设处理（Diagnostic）
- 抗投机能力
- 迭代与停止条件

评分应服务于自检，不要默认向用户输出冗长评分表。

---

## 生成流程

### 第一步：判断适合性

参见顶部触发条件。不适合则建议其他模式，不要强行生成。

### 第二步：分类任务

按 Profile 自动选择规则确定使用 Standard 还是 Diagnostic Goal。

### 第三步：提取与分类

从用户输入中提取：
- **事实**：有证据支持的现象
- **要求**：用户明确的目标
- **假设**：线索/猜测/建议
- **缺失信息**：影响验收但尚未获得的内容

### 第四步：选择默认策略

根据 Default-First 和 Discovery-First 策略，为低风险不确定性选择保守默认值。

### 第五步：生成 Goal

按所选 Profile 模板生成。

### 第六步：质量自检

逐项检查对应 Profile 的质量门禁。

### 第七步：输出

按输出契约输出完整 Goal。

---

## 何时使用，何时不使用

**适合 → 生成 Goal：**
- 完成条件相对明确
- 实现路径需要根据调查或实验动态决定
- 存在可检查的测试、日志、benchmark、生成物或其他证据

**不适合 → 建议其他模式：**
- 一次性修改、纯探索、尚未确定方向的架构讨论 → 普通 Prompt 或 Plan Mode
- 任务模糊到无法定义任何完成条件 → 先 Plan
- 简单翻译、一行 shell 输出、纯头脑风暴 → 直接回答

---

## 常见失败模式

参见 `references/failure-modes.md`（包含真实失败案例及其修正版本）。

---

## 参考文件

- `references/standard-goal-examples.md` — Standard Goal 示例
- `references/diagnostic-goal-examples.md` — Diagnostic Goal 示例
- `references/default-reference.md` — 默认策略参考
- `references/discovery-reference.md` — Discovery-first 参考
- `references/failure-modes.md` — 常见失败模式
- `scripts/lint_goal.py` — 轻量静态校验脚本
