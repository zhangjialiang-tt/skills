# L2 验证计划 — {MODULE_NAME}

> 由 Explore sub-agent 基于 RTL 源码分析生成。用户应 review 并修改后确认。

## 1. 功能摘要

{一句话描述 DUT 的核心功能}

**输入：** {数据来源/上游接口}
**输出：** {数据去向/下游接口}
**核心操作：** {DUT 做了什么处理}

## 2. 接口分析

### 2.1 时钟/复位

| 信号 | 方向 | 说明 |
|------|------|------|
| clk | input | 系统时钟 |
| rst | input | 异步复位 |

### 2.2 控制流

| 接口 | 方向 | 协议 | 验证重点 |
|------|------|------|----------|
| {信号组} | {方向} | {协议类型} | {需要验证什么} |

### 2.3 数据输入

| 接口 | 方向 | 协议 | 数据格式 | 验证重点 |
|------|------|------|----------|----------|
| {信号组} | input | {握手/流式} | {数据宽度/含义} | {何时输入、输入什么} |

### 2.4 数据输出

| 接口 | 方向 | 协议 | 数据格式 | 验证重点 |
|------|------|------|----------|----------|
| {信号组} | output | {握手/流式} | {数据宽度/含义} | {输出内容是否正确} |

### 2.5 状态/调试输出

| 信号 | 方向 | 含义 | 验证用途 |
|------|------|------|----------|
| {信号} | output | {描述} | {用于判断什么} |

## 3. 状态机覆盖

### 3.1 状态列表

| 状态名 | 编码 | 含义 |
|--------|------|------|
| S_xxx | N'dX | {状态功能描述} |

### 3.2 状态转移路径

| 转移 | 触发条件 | 验证方法 |
|------|----------|----------|
| S_A → S_B | {触发信号/条件} | {如何构造激励使状态转移} |

### 3.3 状态覆盖目标

- [ ] 主路径完整遍历
- [ ] 每个状态至少进入一次
- [ ] 错误状态进入和退出
- [ ] 所有状态转移边

## 4. 参数/可配置项

| 参数 | 默认值 | 合法范围 | 验证策略 |
|------|--------|----------|----------|
| {NAME} | {default} | {min..max / 枚举} | {测试哪些值} |

## 5. 测试用例

> 原则：覆盖主路径、变体场景、错误注入、边界条件。用例数量适中（3-8 个）。

| TC# | 名称 | 场景描述 | 输入激励 | 期望输出 | Checker 方法 | 优先级 |
|-----|------|----------|----------|----------|--------------|--------|
| TC01 | 典型主路径 | {正常工作的典型场景} | {具体输入描述} | {期望输出} | {如何判定 PASS} | P0 |
| TC02 | {变体} | {参数/配置变化} | {输入} | {输出} | {方法} | P0 |
| TC03 | {变体} | {数据模式变化} | {输入} | {输出} | {方法} | P1 |
| TC04 | 错误注入 | {触发错误条件} | {输入} | {期望错误码} | {采样错误输出} | P1 |
| TC05 | 边界条件 | {FIFO 满/空、计数器溢出等} | {输入} | {期望行为} | {方法} | P1 |
| TC06 | 复位 | {中途复位} | {输入} | {期望恢复} | {状态检查} | P2 |
| TC07 | {自定义} | {用户关心的场景} | {输入} | {输出} | {方法} | P2 |

### TC01 详细描述（展开最核心的用例）

**目的：** {验证什么}
**激励步骤：**
1. {步骤 1：如"等待 10 周期后置起 i_frame_start"}
2. {步骤 2：如"连续输入 N 个 block result，每个间隔 2 周期"}
3. {步骤 3：如"输入 param 流"}
4. {步骤 4：如"等待 DMA 输出完成"}

**期望结果：**
- {信号A} = {期望值}（在 {时间点}）
- {信号B} = {期望值}

**Checker 实现：**
- {在哪个时钟沿采样、比较什么}

## 6. Checker 策略

### 6.1 检查方法

| 检查类型 | 适用信号 | 实现方式 |
|----------|----------|----------|
| 数值比较 | {信号} | 直接采样 vs 期望值 |
| 协议检查 | {接口} | 时序/握手合法性断言 |
| 数据重建 | {输出} | 抓取所有输出数据，重建完整帧/包 |
| 错误码检查 | {信号} | 比较 o_error_code vs 期望错误码 |
| 状态转移检查 | 内部状态（如有 probe） | 验证状态机路径 |

### 6.2 数据重建策略（如适用）

如果 DUT 输出需要通过多次 DMA/写事务重建完整数据：

```
抓取方法：在每个 clk 上升沿，当 {输出 valid/ready 握手成功} 时，记录 {tdata, tkeep, tlast}
重建逻辑：按地址偏移累加，组装完整 {帧/包/块}
对比方式：与期望 {帧/包/块} 逐字节/逐字比较
```

### 6.3 Golden/Reference（如适用）

- [ ] 有现成 Python/C reference model → 路径：{path}
- [ ] 需要新建 reference model → 说明：{如何实现}
- [ ] 无 reference，用数值比较替代 → 说明：{期望值从哪来}

## 7. 风险/限制

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| {如：复杂状态机某些状态难以到达} | {无法覆盖} | {通过特定输入序列强制进入} |
| {如：DMA 输出内容依赖多个参数组合} | {checker 复杂} | {只检查关键参数组合} |
| {如：时序依赖不明确} | {激励时机不确定} | {从 RTL 注释/波形推断} |

## 8. 覆盖目标汇总

- [ ] 主功能路径
- [ ] 状态机全覆盖
- [ ] 错误码全覆盖
- [ ] 参数边界
- [ ] 复位/异常恢复
- [ ] 数据完整性（输入→输出无丢失/无篡改）

---

## 使用说明

1. **此计划由 AI 自动生成**，基于 RTL 注释和源码结构。用户必须 review 修正。
2. 修改测试用例的期望值、增删用例、调整优先级后保存。
3. 确认后，main agent 将根据此计划生成 L2 testbench。
4. 如果 DUT 功能与计划不符，修改计划比修改 TB 更高效。
